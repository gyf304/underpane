{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all monitor windows",
  "windows": ["monitor-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
